const fs = require("fs");
const os = require("os");
const cp = require("child_process");

let output;
try {
  exec()
} catch (e) {
  console.warn(
    "Please contact developers."
  );
  process.exit(1);
}
